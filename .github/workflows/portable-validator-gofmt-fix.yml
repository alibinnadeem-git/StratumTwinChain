name: STRATUM Portable Validator Gofmt Fix

on:
  push:
    branches:
      - redbook/povi-proof-verifying-sync-runtime-v1
    paths:
      - '.github/workflows/portable-validator-gofmt-fix.yml'

permissions:
  contents: write

jobs:
  gofmt-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: redbook/povi-proof-verifying-sync-runtime-v1
          fetch-depth: 0
      - uses: actions/setup-go@v6
        with:
          go-version: '1.24.x'
          cache: false
      - name: Canonical gofmt normalization
        run: gofmt -w tools/portable-validator/*.go
      - name: Commit formatting-only normalization
        run: |
          if git diff --quiet -- tools/portable-validator/*.go; then
            echo 'No Go formatting changes required.'
            exit 0
          fi
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tools/portable-validator/*.go
          git commit -m 'Normalize portable validator Go formatting'
          git push origin HEAD:redbook/povi-proof-verifying-sync-runtime-v1
