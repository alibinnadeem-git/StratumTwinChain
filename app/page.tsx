import Link from 'next/link';import {assets,events,projects} from '@/lib/data';import AssetPassport from '@/components/AssetPassport';export default function Home(){const verified=assets.filter(a=>a.block).length;return <><div className="topline"><div><div className="eyebrow">Infrastructure Trust Operating System</div><h1 className="title">Know what exists. Know what happened. Prove it.</h1><div className="subtitle">STRATUM Twin represents the infrastructure. STRATUM Verified captures provenance and lifecycle. DIR — Digital Immutable Records — makes approved proof independently immutable.</div></div><div className="badge">● DIR healthy · stratum-devnet-1</div></div><section className="hero"><div className="card hero-copy"><div className="eyebrow">Physical → Digital → Verified</div><h2>One durable identity for every installed asset.</h2><p className="muted">Procurement, custody, installation, inspection, commissioning, documents, maintenance and replacement become one cryptographically verifiable asset history.</p><div className="button-row"><Link className="action" href="/twin">Open layered Twin</Link><Link className="ghost" href="/dir">DIR Explorer</Link></div></div><div className="card layer-visual"><div className="layer l1"><b>L0–L3</b><span>Source + Design Layers</span></div><div className="layer l2"><b>L4</b><span>STRATUM Assets</span></div><div className="layer l3"><b>L5–L7</b><span>Installation + Evidence + Operations</span></div><div className="layer l4"><b>L8</b><span>STRATUM Verified</span></div><div className="layer l5"><b>DIR</b><span>Digital Immutable Records</span></div></div></section><section className="grid kpis"><div className="card"><div className="label">Tracked Assets</div><div className="metric">{projects.reduce((n,p)=>n+p.assets,0).toLocaleString()}</div><div className="muted">Across {projects.length} active portfolios</div></div><div className="card"><div className="label">Immutable Records</div><div className="metric">18,943</div><div className="muted">Signed lifecycle records</div></div><div className="card"><div className="label">Passport Coverage</div><div className="metric">{Math.round(verified/assets.length*100)}%</div><div className="muted">Demo asset cohort finalized</div></div><div className="card"><div className="label">Pending Approvals</div><div className="metric">5</div><div className="muted">QA / commissioning queue</div></div></section><section className="grid two"><div><div className="section-head"><div><div className="eyebrow">Live Asset Passport</div><h2>Infrastructure provenance at a glance</h2></div></div><AssetPassport asset={assets[0]}/></div><div className="card"><div className="section-head"><div><div className="eyebrow">Activity</div><h3>Verification stream</h3></div></div><div className="timeline">{events.map(e=><div className="event" key={e.meta}><i className={`event-icon ${e.kind}`}>✓</i><div><strong>{e.title}</strong><small>{e.meta}</small></div></div>)}</div></div></section></>}
