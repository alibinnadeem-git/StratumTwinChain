import {recentChain} from '@/lib/server/live-views';
export const dynamic='force-dynamic';
async function rpcStatus(){const rpc=(process.env.STRATUM_CHAIN_RPC_URL||'').replace(/\/$/,'');if(!rpc)return{connected:false,chainId:process.env.STRATUM_CHAIN_ID||'stratum-devnet-1',height:0,latestBlockHash:'Not connected',validators:[],quorumPower:0,nativeDenom:'ustrm',displayDenom:'STRM'};try{const r=await fetch(`${rpc}/v1/status`,{cache:'no-store'});if(!r.ok)throw new Error();return{...await r.json(),connected:true}}catch{return{connected:false,chainId:process.env.STRATUM_CHAIN_ID||'stratum-devnet-1',height:0,latestBlockHash:'RPC unavailable',validators:[],quorumPower:0,nativeDenom:'ustrm',displayDenom:'STRM'}}}
const short=(s:string|null,n=15)=>s?s.length>n*2?`${s.slice(0,n)}…${s.slice(-n)}`:s:'—';
export default async function ChainPage(){let db:any={state:{},blocks:[]};try{db=await recentChain();}catch{}const s:any=await rpcStatus();const height=Number(db.state?.height||s.height||0);return <><div className="page-head"><div><div className="eyebrow">Digital Immutable Records System · DIRS</div><h1 className="title">DIRS Explorer</h1><div className="subtitle">Independent immutable lifecycle records for approved STRATUM Verified events. DIRS keeps private project content outside the trust layer while preserving verifiable hashes, record identifiers, timestamps and validator-backed finalization.</div></div><span className={s.connected?'badge':'pending'}>{s.connected?'DIRS ONLINE':'DIRS RPC OFFLINE'}</span></div><div className="grid kpis"><div className="card"><div className="label">DIRS Network</div><div className="metric" style={{fontSize:20}}>{s.chainId||db.state?.chain_id}</div></div><div className="card"><div className="label">Finalized Record Height</div><div className="metric">{height}</div></div><div className="card"><div className="label">Validator Authorities</div><div className="metric">{s.validators?.length||0}</div></div><div className="card"><div className="label">Record State</div><div className="metric" style={{fontSize:20}}>{height>0?'Immutable':'Genesis'}</div></div></div><div className="grid two"><div className="card"><div className="section-head"><div><div className="label">Finalization State</div><h2>Latest immutable record group</h2></div><span className="proof">{height>0?'QUORUM FINALIZED':'GENESIS'}</span></div><div className="verify-grid"><span>Record height</span><b>{height}</b><span>Record hash</span><b className="mono">{short(db.state?.latest_block_hash||s.latestBlockHash,22)}</b><span>Genesis</span><b className="mono">{short(db.state?.genesis_hash,22)}</b><span>Required authority</span><b>{s.quorumPower||2} of 3</b></div></div><div className="card"><div className="label">Validator Authorities</div><h2>Independent signers</h2><div className="timeline">{(s.validators||[]).map((v:any)=><div className="event" key={v.address}><i className="event-icon">✓</i><div><strong>{v.name}</strong><small className="mono">{v.address}</small></div></div>)}{!(s.validators||[]).length&&<div className="muted">Validator status will appear when the DIRS RPC is connected.</div>}</div></div></div><div className="card table-card" style={{marginTop:16}}><div className="section-head"><div><div className="eyebrow">Immutable Record Explorer</div><h2>Finalized proof records</h2></div><span className="proof">{db.blocks.length} shown</span></div><table className="table"><thead><tr><th>Record #</th><th>Event / Record</th><th>Reference</th><th>Evidence Hash</th><th>Authority</th><th>Votes</th></tr></thead><tbody>{db.blocks.map((b:any)=><tr key={b.block_hash}><td><strong>#{b.height}</strong><div className="muted">{new Date(b.finalized_at).toLocaleString()}</div></td><td>{b.event_type||'Proof'}<div className="muted mono">{short(b.record_id,12)}</div></td><td className="mono" title={b.tx_hash}>{short(b.tx_hash,12)}</td><td className="mono" title={b.evidence_hash||''}>{short(b.evidence_hash,12)}</td><td>{b.proposer_validator_id}</td><td><span className="proof">{Array.isArray(b.votes_json)?b.votes_json.length:0}/3</span></td></tr>)}{!db.blocks.length&&<tr><td colSpan={6}><span className="muted">No finalized DIRS records beyond genesis yet.</span></td></tr>}</tbody></table></div><div className="card" style={{marginTop:16}}><div className="label">Privacy Boundary</div><h2>Private evidence stays outside DIRS</h2><div className="subtitle">DIRS stores proof identifiers, event type, evidence-package SHA-256, canonical payload SHA-256, signer reference and timestamp. Photos, drawings, contracts and customer files remain in private application storage.</div></div></>}
